import Foundation

extension Images {
    enum SystemSymbol {
        enum Chevron {
            // swiftlint:disable:next identifier_name
            static let up = "chevron.up"

            static let down = "chevron.down"
        }
    }
}
